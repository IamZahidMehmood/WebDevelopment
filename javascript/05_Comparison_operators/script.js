// ==,!=,<,>,<=,>=
const userAge1='21'
const userAge2=21
// ==, === in == it will convert them into same datatype and then compare there values
// === this does not convert there data types , there data types as well values must be the same for true output
